# Static Talli invite-fallback site, deployed with `wrangler deploy`.
name = "talli-web"
compatibility_date = "2026-05-22"

[assets]
directory = "./dist"
# Preserve the Pages-style SPA fallback used by /join/<token>.
not_found_handling = "single-page-application"
